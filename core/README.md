# <Project name>

<One paragraph: what it does, who uses it, why it exists.>

## Install

```bash
# see docs/STACK.md
```

## Run

```bash
```

## Test

```bash
```

## Deploy

<How, where, who.>

## Project structure

See `docs/ARCHITECTURE.md`. Decisions are in `docs/DECISIONS/`. Changes are in `docs/CHANGELOG.md`.

## Working on this project with Claude Code

Standards are in `CLAUDE.md` and `.claude/`. Start with `/plan-project` for anything non-trivial, implement with `/tdd`, finish with `/definition-of-done`.
